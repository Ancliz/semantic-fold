import type { CollapseFilter } from "../model/filters";
import type { RegionNode } from "../model/region";

export function filterRegions(rootNodes: RegionNode[], _filter: CollapseFilter): RegionNode[] {
    return rootNodes;
}